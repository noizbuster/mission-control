{
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "name": "config",
  "projectType": "library",
  "root": "packages/config",
  "sourceRoot": "packages/config/src",
  "implicitDependencies": ["protocol"],
  "targets": {
    "build": {
      "executor": "nx:run-commands",
      "outputs": ["{projectRoot}/dist"],
      "options": {
        "cwd": "packages/config",
        "command": "vite build && tsc -p tsconfig.json --emitDeclarationOnly"
      }
    },
    "typecheck": {
      "executor": "nx:run-commands",
      "options": {
        "cwd": "packages/config",
        "command": "tsc -p tsconfig.json --noEmit"
      }
    },
    "test": {
      "executor": "nx:run-commands",
      "options": {
        "command": "vitest run packages/config/src --passWithNoTests --reporter verbose"
      }
    }
  }
}
